# Design files
*.psd filter=lfs diff=lfs merge=lfs -text
*.ai binary

# Other binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# Notes: JPG, PNG, WEBP, GIF are now untracked from LFS to support direct serving via GitHub Pages.
